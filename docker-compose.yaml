version: '3'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      - db
    restart: always

  db:
    env_file:
      - config/.env.local
    image: postgres:latest
    ports:
      - "5432:5432"